syntax = "proto3";
package earthproto.earthproto;

import "gogoproto/gogo.proto";
import "earthproto/earthproto/params.proto";

option go_package = "earthProto/x/earthproto/types";

// GenesisState defines the earthproto module's genesis state.
message GenesisState {
  Params params = 1 [(gogoproto.nullable) = false];
}
